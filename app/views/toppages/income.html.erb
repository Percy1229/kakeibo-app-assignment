<!--ここにindex.htmlと同じコードを書く-->
<p>Total Balance: ¥<%= @total %></p>

<ul class="nav nav-tabs nav-justified">
    <li class="nav-item">
        <a href="<%= root_url %>" class="nav-link 
        <%= 'active' if current_page?(root_url) %>">
            Expense
        </a>
    </li>
    <li class="nav-item">
        <a href="<%= toppages_income_path %>" class="nav-link 
        <%= 'active' if current_page?(toppages_income_path) %>">
            Income
        </a>
    </li>
</ul>
<%= render 'incomes/income', incomes: @incomes, income_total: @income_total %>